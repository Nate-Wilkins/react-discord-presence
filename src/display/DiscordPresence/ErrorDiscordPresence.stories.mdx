import { Meta, Description, Story } from '@storybook/addon-docs';
import { DiscordPresenceClassesDefault } from '../style';
import { ThemeDiscordPresence } from './ThemeDiscordPresence';
import { ErrorDiscordPresence } from './ErrorDiscordPresence';

<Meta title="Display/ErrorDiscordPresence" component={ErrorDiscordPresence} argTypes={{ }} />

export const Template = (args) => (
  <div style={{ fontSize: '16px' }}>
    <ThemeDiscordPresence
      classes={DiscordPresenceClassesDefault}
      theme={args.theme}
    >
      <ErrorDiscordPresence error="Unkown error description." />
    </ThemeDiscordPresence>
  </div>
)

# ErrorDiscordPresence

Display error state for Discord presence.

## Default Theme

<Story name="Default" args={{ theme: { primary: '#36393f', accent: '#2f3136' } }}>
  {Template.bind({})}
</Story>

## Custom Theme

<Story name="Custom" args={{ theme: { primary: '#2672c3', accent: '#001a30' } }}>
  {Template.bind({})}
</Story>

